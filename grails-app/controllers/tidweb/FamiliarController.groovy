package tidweb

/**
 * FamiliarController
 * A controller class handles incoming web requests and performs actions such as redirects, rendering views and so on.
 */
class FamiliarController {

	def scaffold = Familiar
//	def index = { }
}
