package tidweb

/**
 * MuralController
 * A controller class handles incoming web reque and performs actions such as redirects, rendering views and so on.
 */
class MuralController {

	def scaffold = Mural
//	def index = { }
}
